# Procfile for Railway (if deploying as single service)
# Railway will use this to start multiple processes

# Note: Railway doesn't support multiple processes in one service well
# This is for reference only - use separate services instead

web: node Robridge\ web/server.js
ai: python server.py
flask: python Barcode\ generator\&Scanner/start_server.py

